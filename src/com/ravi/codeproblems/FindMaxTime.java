package com.ravi.codeproblems;

import java.util.List;

/* 
 * 
 * 
 */
public class FindMaxTime {

	/*public static void main(int a, int b, int c, int d)
	
	public String findMaxTime(List inputList){
		List<Integer> endList = null; 
		endList.add(inputList.stream().filter((x -> x < 2)).findFirst().get());
		endList.add(getSecondElement(inputList))
		endList.add*/
		
	}
	/*
	 * Write 4 lambda Functions and use it on the list
	 * iterate over each digit [i] and search through the list until you find the highest number that fits the requirements.
		[0] must be <=2
		
		[1] must be <=3 if [0]==2
		
		[2] must be <=5
		
		[3] can be anything
	 */
	
	 
}
